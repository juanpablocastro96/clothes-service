name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 ${{ github.hash }} event."
      #- run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      #- run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      #- run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      #- run: echo "🍏 This job's status is ${{ job.status }}."
      #steps:
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Build with Maven
        run: mvn clean install
      - name: Terraform installation
        uses: hashicorp/setup-terraform@v2
      #- name: Checking Terraform installation
        #run: terraform -help
      #- name: Testing cd commands in GitHub Actions
        #run: ls -la
      #- name: Testing bash file
        #run: bash test-file.sh
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Testing docker AFTER LOGIN
        run: docker build . -t juancastro96/personal-repo:example5
     # - name: Testing docker tag
      #  run: docker tag example1 juancastro96/personal-repo:example1
      #- name: Testing docker image ls
        #run: docker images/ docker ps
      - name: Testing docker push
        run: docker push juancastro96/personal-repo:example5